Sources for the plugin `plugin.collisionFilters`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.collisionFilters" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
